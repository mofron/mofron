

mofron.appframe.Loading = class extends mofron.appframe.Base {
    constructor (img) {
        try {
            super();
            this.image  = img;
        } catch (e) {
            console.error(e.stack);
            throw e;
        }
    }
    
    init (disp) {
        try {
            
        } catch (e) {
            console.error(e.stack);
            throw e;
        }
    }
}
