/**
 * @file theme.js
 * @brief app theme
 */
const mf   = require('mofron');

module.exports = {
    init : (thm) => {
        try {
            // please define app theme here
        } catch (e) {
            console.error(e.stack);
            throw e;
        }
    }
};
/* end of file */
