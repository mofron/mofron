module.exports = {
    entry: __dirname + '/entry.js',
    output: {
        path: __dirname + '/dist',
        filename: 'mofron.js'
    },
    module: {
        loaders: [{
            test: /\.js$/,
            exclude: /node_modules/,
            loader: 'babel-loader',
            query: {
                presets: ['es2015']
            }
        }]
    }
};
